# Transport switch for the MOCForge API (CONTRACT.md v1).
#
#   unset or "mock"  -> the in-process typed mock layer (src/lib/api/mock)
#   an http(s) origin -> the real service, called at <origin>/api/v1
#
# Nothing else in the client changes between the two.
NEXT_PUBLIC_MOCFORGE_API=mock
